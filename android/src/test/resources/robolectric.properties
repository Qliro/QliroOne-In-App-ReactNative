# Pinned rather than inherited from targetSdk, for the same reason as the native SDK's own
# src/test/resources/robolectric.properties: Robolectric ships one android-all jar per released SDK
# level, so following compileSdk/targetSdk up would break these tests the moment we bump to a level
# Robolectric has not shipped yet. Nothing under test here varies by SDK level.
sdk=34
